import 'package:flutter/material.dart';

final ColorScheme colorScheme = const ColorScheme.dark().copyWith(
  primary: const Color(0xFF00796B), // Primary color (dark teal)
  secondary: const Color(0xFFECC94B), // Secondary color (mustard yellow)
  tertiary: const Color(0xFFD3D3D3), // Tertiary color (light gray)
  background: const Color(0xFF121212), // Background color (dark black)
  surface: const Color(0xFF1E1E1E), // Surface color (dark gray)
  error: const Color(0xFFCF6679), // Error color (pinkish red)
);

